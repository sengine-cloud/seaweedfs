# Builds this fork's seaweedfs image into the org OCI registry.
#
# Exists because seaweedfs/seaweedfs#11140 is not in a release yet: an entry
# rewritten with identical content before it was ever replicated is never
# uploaded to the remote tier. See docker/Dockerfile.sengine — this is the
# published 4.41 image with one rebuilt binary, not a fork of the release
# pipeline. The cluster should not run an image built on somebody's laptop.
#
# workflow_dispatch only, deliberately. This repo is a PUBLIC fork, and the org
# OCI credential is visible to every repo in the org, so there must be no event
# here that an outside contributor can trigger. No pull_request, no push.
#
# Runs on a GitHub-hosted runner rather than the shared ARC pool: public repos
# cannot reach it. That costs billable minutes, which is the other reason this
# is on-demand rather than on every push.
name: oci-image (sengine)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to build (defaults to this branch)
        required: false
        default: ""

permissions:
  contents: read

env:
  DOCKER_BUILD_SUMMARY: "false" # buildx otherwise uploads a *.dockerbuild artifact per run
  IMAGE_NAME: seaweedfs

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref || github.ref }}

      - uses: docker/setup-buildx-action@v3

      - name: Log in to the org OCI registry
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.OCI_REGISTRY_HOST }}
          username: ${{ vars.OCI_REGISTRY_LOGIN }}
          password: ${{ secrets.OCI_REGISTRY_PASSWORD }}

      - name: Compute short SHA
        id: vars
        run: echo "sha=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      # No :latest. These images are pinned by digest in fluxcd-infra, and a
      # floating tag on a fork invites exactly the drift the pin exists to stop.
      - name: Build and push
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/Dockerfile.sengine
          push: true
          platforms: linux/amd64
          tags: ${{ vars.OCI_REGISTRY_HOST }}/${{ vars.OCI_REGISTRY_PREFIX }}/${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.sha }}

      - name: Report the digest to pin
        run: |
          echo "### Image pushed" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          echo "${{ vars.OCI_REGISTRY_HOST }}/${{ vars.OCI_REGISTRY_PREFIX }}/${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.sha }}@${{ steps.build.outputs.digest }}" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
